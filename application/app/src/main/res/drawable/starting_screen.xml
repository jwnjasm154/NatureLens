<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape>
            <!-- black background -->
            <solid android:color="@color/main_app" />
        </shape>
    </item>
    <!-- launcher icon -->
    <item android:drawable="@drawable/ic_launcher_foreground" android:height="150dp" android:width="150dp" android:gravity="center" />
</layer-list>